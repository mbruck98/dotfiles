dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds config
    preserve_split = true # You probably want this
    force_split = 2
}

master {
    new_status = master
}
